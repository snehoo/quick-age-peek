/**
 * Cloudflare Workers static assets config for whatismyage.me
 * Static Vite site — build output (dist/) is deployed as Worker assets.
 * Uses _worker.js in project root for redirects (www canonicalization & trailing slashes).
 * Cloudflare Pages automatically picks up _worker.js as middleware.
 */
{
  "name": "quick-age-peek",
  "compatibility_date": "2026-06-24",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
