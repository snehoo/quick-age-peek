# Cloudflare Pages _redirects file
# Note: Only relative URLs are allowed in this file
# www to non-www redirect is handled through Cloudflare Bulk Redirect Rules in the dashboard

# Example relative redirect (if needed):
# /old-page /new-page 301
